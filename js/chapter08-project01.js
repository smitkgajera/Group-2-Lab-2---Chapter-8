/* add loop and other code here ... in this simple exercise we are not
   going to concern ourselves with minimizing globals, etc */
   var title = $('<h1>Shopping Cart</h1>');

   $(function(){
      $(".title").empty();
      $(".title").append(title); // just replacing title
      }   
   );


